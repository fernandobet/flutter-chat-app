import 'package:flutter/material.dart';

class ChatPage extends StatelessWidget {
  static final ruta = 'chat';
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}